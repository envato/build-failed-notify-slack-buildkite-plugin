name: Build Failed Notify Slack Buildkite Plugin
description: "@'s the creator of the failed build via a mapping file"
author: https://github.com/envato/build-failed-notify-slack-buildkite-plugi
requirements:
  - jq
configuration:
  properties:
    mapping_file:
      type: string
    channel:
      type: string
    notify_on_state_change:
      type: boolean
  required:
    - mapping_file
    - channel
  additionalProperties: false
